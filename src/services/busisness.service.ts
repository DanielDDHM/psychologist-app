import { PatientTypes } from "../types/patient.types"
import { PsyTypes } from "../types"

export namespace PsychologistService {
  export const register = async (params: PsyTypes.register) => { }
}

export namespace PatientService {
  export const register = async (params: PatientTypes.register) => { }
}